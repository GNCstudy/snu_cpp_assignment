- 소수(n)는 2부터 sqrt(n) 까지의 정수로 나누었을때 나누어떨어지지 않는다면 소수라고 판단할 수 있다.

모순을 위한 가정: sqrt(n) 보다 큰 정수 a로 나누어 떨어진다.
-> a로 나누어 떨어진다면 n/a로도 나누어 떨어진다.   a*(n/a)=n
-> n/a는 sqrt(n)보다 작은 정수이므로 모순이 발생한다.

따라서 sqrt(n)이하의 정수로 나누어떨어지지 않는다면 소수라고 판단할 수 있다.
